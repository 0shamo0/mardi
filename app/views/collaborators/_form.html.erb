<div class="container">
  
<%= form_with(model: collaborator, local: true) do |form| %>
  <% if collaborator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(collaborator.errors.count, "error") %> prohibited this collaborator from being saved:</h2>

      <ul>
      <% collaborator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row">
    <%= form.label :Nom_du_collaborateur, :class => "col-sm-2 col-form-label" %>
  <div class="col-sm-10">
    <%= form.text_field :collaborator_name, :class => "form-control" %>
  </div>
  </div>
  

  
  <div class="form-group row">
    <%= form.label :Email_du_collaborateur, :class => "col-sm-2 col-form-label" %>
  <div class="col-sm-10">
    <%= form.text_field :collaborator_email, :class => "form-control" %>
  </div>
  </div>

  <div class="actions">
    <%= form.submit :Validation, class: 'btn btn-primary' %>
  </div>
<% end %>
</div>