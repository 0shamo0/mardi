<div class="container">
<%= form_with(model: project, local: true) do |form| %>
  <% if project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Nom_du_projet, :class => "col-sm-2 col-form-label" %>
  <div class="col-sm-10">
    <%= form.text_field :project_name, :class => "form-control" %>
  </div>
  </div>

  <div class="actions" style="margin-top: 30px; margin-left: 15px">
    <%= form.submit :Validation, class: 'btn btn-primary' %>
  </div>
<% end %>
</div>

