<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto" style="height: 60px; padding-top: 8px">
          <%= link_to image_tag("logo-2.png", :alt => "Image Description", :class => "width: 200px"), root_path %>
        </ul>
        
        <ul class="navbar-nav ml-auto">
              
              
        <% if user_signed_in? && current_user.admin? %>
              <li class="nav-item">
                <div class="nav-link"><%= link_to 'collaborateurs', collaborators_path %></div>
              </li>
              <li class="nav-item">
                <div class="nav-link" style="display: none"><%= link_to 's\'inscrire', new_user_registration_path %></div>
              </li>
        <% end %>      
              
        <% if user_signed_in? %>
              <li class="nav-item">
                <div class="nav-link"><%= link_to 'projets', projects_path %></div>
              </li>
              <li class="nav-item">
                <div class="nav-link"><%= link_to 'tâches', tasks_path %></div>
              </li>
              <li class="nav-item">
                <div class="nav-link"><%= link_to 'se deconnecter', destroy_user_session_path, method: :delete %></div>
              </li>
        <% end %>
        </ul>
      </div>
    </div>
</nav>